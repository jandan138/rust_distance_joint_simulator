use bevy::prelude::*;

fn main() {
    // 设置Bevy应用
    App::new()
        .add_plugins(DefaultPlugins)
        .run();
}

// fn setup(commands: &mut Commands) {
//     // 初始化场景和对象
//     commands.spawn(); // 添加Cuboid等
// }
